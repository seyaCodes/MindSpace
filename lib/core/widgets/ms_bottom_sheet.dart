import 'package:flutter/material.dart';

class MsBottomSheet extends StatelessWidget {
  final Widget child;

  const MsBottomSheet({super.key, required this.child});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
