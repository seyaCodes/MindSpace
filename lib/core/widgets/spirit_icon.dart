import 'package:flutter/material.dart';

class SpiritIcon extends StatelessWidget {
  final Color color;
  final double size;

  const SpiritIcon({super.key, required this.color, this.size = 24});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
