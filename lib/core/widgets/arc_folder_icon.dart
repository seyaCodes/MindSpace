import 'package:flutter/material.dart';

class ArcFolderIcon extends StatelessWidget {
  final Color color;
  final double size;

  const ArcFolderIcon({super.key, required this.color, this.size = 48});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
