import 'package:flutter/material.dart';

class NewThreadCard extends StatelessWidget {
  const NewThreadCard({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
