import 'package:flutter/material.dart';

class LastTimeCard extends StatelessWidget {
  const LastTimeCard({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
