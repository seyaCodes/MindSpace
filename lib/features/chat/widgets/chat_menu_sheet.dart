import 'package:flutter/material.dart';

class ChatMenuSheet extends StatelessWidget {
  const ChatMenuSheet({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
