import 'package:flutter/material.dart';

class TurningPointCard extends StatelessWidget {
  const TurningPointCard({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
