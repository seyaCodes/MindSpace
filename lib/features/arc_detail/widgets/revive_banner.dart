import 'package:flutter/material.dart';

class ReviveBanner extends StatelessWidget {
  const ReviveBanner({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
