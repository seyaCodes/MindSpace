import 'package:flutter/material.dart';

class ArcSessionCard extends StatelessWidget {
  const ArcSessionCard({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
