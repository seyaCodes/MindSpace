import 'package:flutter/material.dart';

class ArcAnalysisPreviewCard extends StatelessWidget {
  const ArcAnalysisPreviewCard({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
